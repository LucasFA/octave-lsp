[package]
name = "octave-lsp"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
parser = {path = "../parser"}

[dev-dependencies]
rusty-hook = "^0.11.2" # Enforces cargo fmt throughout the project (beyond this crate!)