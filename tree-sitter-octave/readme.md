To develop the grammar, first install [the dependencies](https://tree-sitter.github.io/tree-sitter/creating-parsers#dependencies)

Then you can build the parser and run tests with:

```bash
tree-sitter generate
tree-sitter test
```

