// use lsp_server::{Connection, ExtractError, Message, Request, RequestId, Response};
// // use std::error::Error;
// // use lsp_server::Request;

// pub mod server_req {
//     pub struct ServerRequest {
//         req: lsp_server::Message,
//     }

//     impl ServerRequest {
//         pub fn new(msg: lsp_server::Message) -> Self {
//             Self { msg }
//         }

//         pub fn handle() -> Result<(), Box<dyn Error + Sync + Send>> {
//             match msg {
//                 Message::
//             }
//         }
//     }
// }
